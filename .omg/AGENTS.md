<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-02-25T05:32:43Z | Updated: 2026-02-25T05:32:43Z -->

# .omg

## Purpose
Repository-local runtime state written by `oh-my-gemini` setup and team orchestration flows. This directory is volatile and typically regenerated by command execution.

## Key Files

| File | Description |
|------|-------------|
| `setup-scope.json` | Persisted setup-scope selection (`project` or `user`) and last update timestamp. |

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `state/` | Team run phase, monitor, and worker runtime state artifacts. See `state/AGENTS.md`. |

## For AI Agents

### Working In This Directory
- Treat files here as generated runtime artifacts unless the task explicitly concerns state persistence/debugging.
- Prefer changing source code under `src/state` / `src/team` over hand-editing these files.

### Testing Requirements
- Regenerate state via command execution (`npm run setup`, `npm run omg -- team run ...`) rather than manual fabrication when possible.

### Common Patterns
- JSON metadata files at top-level with nested team-scoped state under `state/team`.

## Dependencies

### Internal
- Produced by installer (`src/installer/scopes.ts`) and orchestrator/state store modules.

### External
- None; local filesystem persistence only.

<!-- MANUAL: -->
